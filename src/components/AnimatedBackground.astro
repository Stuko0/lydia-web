<div class="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true">
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
  <div class="orb orb-4"></div>
  <div class="orb orb-5"></div>
  <div class="orb orb-6"></div>
  <div class="mesh-overlay"></div>
</div>

<style>
  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.22;
    will-change: transform;
  }
  .orb-1 {
    width: 420px;
    height: 420px;
    background: var(--color-rpm-iris);
    top: -10%;
    left: -5%;
    animation: float 22s ease-in-out infinite;
  }
  .orb-2 {
    width: 360px;
    height: 360px;
    background: var(--color-rpm-love);
    top: 30%;
    right: -8%;
    animation: float 26s ease-in-out infinite reverse;
  }
  .orb-3 {
    width: 300px;
    height: 300px;
    background: var(--color-rpm-foam);
    bottom: 10%;
    left: 20%;
    animation: float 20s ease-in-out infinite 2s;
  }
  .orb-4 {
    width: 240px;
    height: 240px;
    background: var(--color-rpm-gold);
    top: 50%;
    left: 40%;
    animation: float 24s ease-in-out infinite 4s;
  }
  .orb-5 {
    width: 200px;
    height: 200px;
    background: var(--color-rpm-pine);
    bottom: 20%;
    right: 25%;
    animation: float 28s ease-in-out infinite 1s;
  }
  .orb-6 {
    width: 180px;
    height: 180px;
    background: var(--color-rpm-rose);
    top: 10%;
    right: 40%;
    animation: float 18s ease-in-out infinite 3s;
  }
  .mesh-overlay {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 20% 20%, rgba(196, 167, 231, 0.08) 0%, transparent 40%),
      radial-gradient(circle at 80% 80%, rgba(235, 111, 146, 0.08) 0%, transparent 40%),
      radial-gradient(circle at 50% 50%, rgba(158, 207, 216, 0.05) 0%, transparent 50%);
  }
  @keyframes float {
    0%, 100% { transform: translate3d(0, 0, 0); }
    33% { transform: translate3d(40px, -60px, 0); }
    66% { transform: translate3d(-30px, 40px, 0); }
  }
</style>
